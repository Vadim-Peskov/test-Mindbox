import { styled, Typography } from "@mui/material";
import { colorActive1 } from "../../../variables";

export const STodosTitle = styled(Typography)(({
  padding: '24px 10px 14px 10px',
  fontSize: '42px',
  textAlign: 'center',
  color: colorActive1,
  fontWeight: '700',
  textTransform: 'uppercase',
}))